import { Meta } from '@storybook/addon-docs';

import { ColorVariables } from './Colours.stories';
import { legacyTokens, solidTokens, uiTokens, wdsTokens } from './tokens-color';

<Meta title={`Global/Colours`} />

# Colour palette

Swatches greater than 50 are accessible when overlayed with white text and when used as the text colour against a white background. Swatches less than 50 are accessible when overlayed with black text.

Please note, colour variables use US English spelling to be consistent with CSS style property names.

## Wellcome Design System colours

<ColorVariables tokens={{...wdsTokens, ...solidTokens}} className={'sb-swatch-grid--wds'} />

## UI colours

<ColorVariables tokens={uiTokens} className={'sb-swatch-grid--ui'}/>

## Legacy colours

<ColorVariables tokens={legacyTokens} className={'sb-swatch-grid--legacy'}/>

## Installation

Colour CSS variables are included in the **core.css** stylesheet.

Import into SASS

```scss
import '@wellcometrust/design-system';

// or 
import '@wellcometrust/design-system/dist/core.css'
```
